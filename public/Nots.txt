root --> index.html içinden geliyor.
<div id="root"></div> body içerisinde sadece root div'i var.
Yani mantık
    - index.html açıldı <div id='root' u gördü. 
    - index.js devreye giriyor.
        - index.js nerede devreye giriyor webpack denilen bir olay ile devreye giriyor isteğe balı araştırabilir.
    - index.js devreye giriyor ReactDom'u render ediyor. (DOM -> uygulamamızın render oluduğunda arka planda çalışan html yapısı)
    - Mantık root'a APP'nin monte edilmesi.

* npm start -> uygulamamızı açmaya yani bir test ortamında çalıştırmaya yarıyor. 

container component denirse bir üst div'e almamız gerekebilir

- App.js içindeki div içindeki kodları sildik.
pages -> sayfalar için pages klasörünü kullanabiliriz
layout -> ekranlar gibi
 - layout içine Dashboad.js componentini ekledik. Baş harfi büyük yazıyoruz Pascalcase
 - rfc --> react functional component snippte ile bir component yaratıyoruz içine Ana sayfa yazdık.
 Bu Dashboad componentini APP içinde çağıralım.
 import Dashboard from './layout/Dashboard';
 <Dashboard /> bu şekilde çağırabiliriz import'u ve çağırma şekli

--------------------------------
telarikc kendo --> kurumsal projelerde kullanılıyor genelde
devexpress -- css kütüphanesi

semantic ui react --> css kütüphanesi
npm install semantic-ui-react semantic-ui-css komutu kurulum yapıyoruz

Dashboad'a bir butona ekledik sitede göründüğü gibi gelmesi içinde import 'semantic-ui-css/semantic.min.css';
importunu ekledik.

1.57.36 axios kurulumu yapılacak
npm install axios --> react'ın kendi içerisinde HTTP requestler yok bizde js dünyasında çok populer olan Axios kütüphanesi kullanıyoruz. API'lere istekte bulunmamızı
sağlan paketimizin ismi. Kendisi bir xml http request.
Bizim uygulamamız üstte bir navitagion, sepeti kategroy ler yanda ürünler bunu şekilde çiz.
navigation, sepet, kategroy'ler layout ile ilgili layoutu yapılandıran componetlerimiz ama ürünler hep değişir kategorler göre forma göre ürünler
bir sayfa diğerleride layoutun içindeki elemanlar olsun.

layout paketi içerisine --> Navi.js, Categories.js ve CartSummary.js dosyları oluşturduk. Bunlara jxs'de diyebiliriz, bir fark yok. O yüzden bunları
jsx'e çevirdik react yazıyoruz diyebiliriz.

Defterdeki şekli çiz

Semantic ui içerisinden bir menü bulduk ve içerisini kopyalayıp Navi içerisine yapıştırdık

2.20.36 devam edilcek semantic ui menü eklendi.

inverted menüye --> menüyü siyah yapıştırdı
fixed --> menü yukarıda sabit kalacak yukarı aşağı inse bile sabit kalcak

Menünün kendisini değil elemenlarını container içine ekledik elemanları ortaladı.

semantic ui 16 eşit parçaya böler
<Grid>
        <GridRow>
          <GridColumn width={4}>
            <Categories />
          </GridColumn>

          <GridColumn width={12}>
            <ProductList />
          </GridColumn>
        </GridRow>
      </Grid>   --> width ile parçalıyabiliyoruz

      3.15.16 css tasarımlarına devam ediliyor

services paketi oluşturduj --> api isteklerimizi veya business kodları burada yazılacak

import axios from 'axios';

export default class ProductService {
    getProducts() {
        return axios.get("http://localhost:8080/api/products/getall") 
    }
}

ProductList sayfası bir data tutucak

hook --> 
const [products, setProduts] = useState([])
useState bize bir nesne döndürüyor onuda districtur edicez, bir data bir fonksiyon dönüyor , benim produtcs diye bir datam var bu sayfada onu kullanıcam 
default değeri boş bir array bu produtcs'ı değiştmek içinde setProduts'ı kullanıyorum buda hook bilgisi oluyor
* produtcs'dan data gelecek

tableRow'u gelen ürün sayısı kadar tekrar etmem gerekiyor
products.map(product => ()) --> bir jsx üret ve onu döndür demek

componentDidMount --> bunun yerine hook kullanıyoruz

useEffect --> life cycle hook u burada çözüyoruz, bu bir fonksiyon, içeriye bir fonksiyon geiyoruz, arrwo function geçebiliriz kısacası component yüklendiğinde
yapılmasını istediğimiz kodu bu blok içerisine yazıyoruz
useEffect(() => {
    
  }) --> sayfa yüklendiğinde bu kod çalışıyor

  productService.getProducts() --> bu bize promis diye bir yapı döndürüyor, başarılı olursa then() başarısız olursa catch()

  cors hatası backend izin verimiyor demek oluyor, backend tanımadığı kaynaklara destek vermiyor backend'de izin vermemiz gerekiyor
  ProductController'da @CrossOring verdik 

  react key değeri istiyoru o yüzde productId'yi verdik